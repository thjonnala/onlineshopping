# Render service URL (matches the service name in render.yaml).
# If you name the Render service differently, update this to match.
REACT_APP_API_URL=https://hyderabad-online-shopping-api.onrender.com/api
